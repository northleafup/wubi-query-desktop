## 1. 图标加载实现

- [x] 1.1 在 `Application.cpp` 中包含 `stb_image.h` 头文件
- [x] 1.2 在 `createWindow()` 中添加图标加载代码，使用 `stbi_load()` 加载 32x32 和 256x256 PNG
- [x] 1.3 调用 `glfwSetWindowIcon()` 设置窗口图标
- [x] 1.4 添加 `stbi_image_free()` 释放像素数据
- [x] 1.5 添加错误处理：图标文件不存在时打印警告并继续

## 2. 验证

- [x] 2.1 构建项目，确认无编译错误
- [x] 2.2 运行程序，检查控制台无图标加载错误
- [x] 2.3 查看系统边栏/Dock，确认显示正确的"五"字图标
